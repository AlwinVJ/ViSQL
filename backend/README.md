# Backend

FastAPI application structured around API, service, repository, and schema boundaries.

Create a virtual environment, install `requirements-dev.txt`, then run `uvicorn app.main:app --reload`. Database migrations use `alembic upgrade head`.

