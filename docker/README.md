# Docker

Docker Compose at the repository root orchestrates the development frontend, backend, and PostgreSQL services. Service Dockerfiles live next to their applications so each image has a minimal build context.

