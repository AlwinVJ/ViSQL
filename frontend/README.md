# Frontend

Next.js 15 App Router application using React 19, TypeScript, Tailwind CSS, shadcn/ui conventions, Framer Motion, React Flow, and Monaco Editor.

Run locally with `npm install && npm run dev`. Use `npm run lint`, `npm run typecheck`, and `npm run build` before merging.

